# بناء رَفّ 3.0.0

## المتطلبات

- Node.js 20 LTS
- npm
- Windows لبناء مثبت NSIS مباشرةً، أو GitHub Actions المرفق

## التحقق

```bash
npm ci
npm test
```

## التشغيل أثناء التطوير

```bash
npm start
```

## إنشاء مثبتات Windows المنفصلة

مثبت 64 بت فقط:

```bash
npm run dist:win:x64
```

مثبت 32 بت فقط:

```bash
npm run dist:win:x86
```

لتنفيذ البناءين بالتتابع:

```bash
npm run dist:win
```

لا يُنشئ الأمر ملفًا جامعًا للمعماريتين؛ تظهر داخل `release` حزمة مستقلة لكل معالج. كما يبني GitHub Actions كل معمارية في Job وArtifact منفصلين.

## نشر إصدار يمكن لرَفّ تحديث نفسه إليه

نظام التحديث يقرأ مباشرة من **GitHub Releases** للمستودع `Hzifa33/raff` ولا يحتاج خادم تحديثات أو قاعدة بيانات خارجية.

عند إنشاء إصدار جديد:

1. حدّث الكود وادفعه إلى GitHub.
2. أنشئ Tag جديدًا أعلى من إصدار البرنامج الحالي، مثل:

```bash
git tag v3.0.1
git push origin v3.0.1
```

3. GitHub Actions يزامن رقم `package.json` أثناء البناء مع رقم الـTag، ويبني مثبتين منفصلين x64 وx86، وينشئ SHA-256 لكل ملف، ثم ينشرها جميعًا في GitHub Release نفسه.
4. أجهزة رَفّ تفحص آخر Release تلقائيًا مرة كل 24 ساعة، ويمكن للمسؤول إجراء فحص يدوي من الإعدادات > النظام.

أسماء الملفات التي يطابقها المحدث تلقائيًا:

```text
Raff-3.0.1-Windows-x64-Setup.exe
Raff-3.0.1-Windows-ia32-Setup.exe
```

يدعم المحدث أيضًا تسمية `x86` لنسخة 32-bit عند البحث داخل Assets.
